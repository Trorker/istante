<script setup lang="ts">import { useClock } from '../composables/useClock';import { useSettingsStore } from '../stores/settings.store';const s=useSettingsStore();const c=useClock(()=>s.data.clock24,()=>s.data.showSeconds)</script><template><section class="clock-block"><div class="greeting">{{c.greeting}}</div><div class="date">{{c.date}}</div><div class="clock">{{c.time}}</div></section></template><style scoped>.clock-block{text-align:center}.greeting{font-size:var(--font-lg);font-weight:560}.date{text-transform:uppercase;letter-spacing:.15em;font-size:var(--font-xs);color:var(--text-secondary);margin-top:.45rem}.clock{font-size:var(--font-display-clock);font-weight:300;line-height:.92;letter-spacing:-.055em;margin-top:.55rem;font-variant-numeric:tabular-nums}</style>
