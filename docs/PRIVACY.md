# Privacy

Le preferenze e i dati applicativi restano nel browser tramite IndexedDB. Gli URL ICS possono contenere token privati: l'interfaccia usa un campo mascherato e il proxy li riceve via POST, non tramite query string.

La geolocalizzazione viene richiesta soltanto per scelta esplicita dell'utente.
