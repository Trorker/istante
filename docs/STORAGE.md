# Storage

Database IndexedDB: `istante`, versione 4.

Store: settings, calendars, calendarEvents, phrases, collections, radioStations, radioSchedules, timerPresets, history, metadata.

`localStorage` è limitato al flag di migrazione iniziale.
