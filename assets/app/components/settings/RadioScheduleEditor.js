import AppIcon from '../common/AppIcon.js';
import { useSettingsStore } from '../../stores/settings-store.js';
import { normalizeSchedule } from '../../services/audio/schedule-service.js';
const DAYS = [['1', 'L'], ['2', 'M'], ['3', 'M'], ['4', 'G'], ['5', 'V'], ['6', 'S'], ['0', 'D']];
export default { name: 'RadioScheduleEditor', components: { AppIcon }, setup() { const s = useSettingsStore().state; const add = () => { const i = s.radioSchedules.length; const presets = [['Mattina', '08:00', '10:00'], ['Pausa pranzo', '12:00', '14:00'], ['Sera', '19:00', '22:00']]; const [name, start, stop] = presets[i] || [`Fascia ${i + 1}`, '09:00', '10:00']; s.radioSchedules = [...s.radioSchedules, normalizeSchedule({ id: `slot-${Date.now().toString(36)}`, name, start, stop, enabled: true, days: '0,1,2,3,4,5,6' }, i)]; }; const remove = index => { s.radioSchedules = s.radioSchedules.filter((_, i) => i !== index); }; const toggleDay = (row, day) => { const set = new Set(String(row.days || '').split(',').filter(Boolean)); set.has(day) ? set.delete(day) : set.add(day); row.days = [...set].sort().join(','); }; return { s, DAYS, add, remove, toggleDay }; }, template: `<div class="schedule-editor"><div class="toggle-list"><label><span><strong>Programmazione radio</strong><small>Accende e spegne la radio nelle fasce configurate.</small></span><input v-model="s.radioScheduleEnabled" type="checkbox"></label></div><div v-if="s.radioScheduleEnabled" class="schedule-list"><article v-for="(row,index) in s.radioSchedules" :key="row.id" class="schedule-row"><div class="schedule-head"><input v-model.trim="row.name" maxlength="48" aria-label="Nome fascia"><label class="toggle-inline">Attiva <input v-model="row.enabled" type="checkbox"></label><button class="icon-button small" type="button" aria-label="Elimina fascia" @click="remove(index)"><AppIcon name="trash"/></button></div><div class="setting-block"><label>Accendi alle<input v-model="row.start" type="time" :disabled="!row.enabled"></label><label>Spegni alle<input v-model="row.stop" type="time" :disabled="!row.enabled"></label></div><div class="weekday-row"><button v-for="day in DAYS" :key="day[0]" type="button" :disabled="!row.enabled" :aria-pressed="row.days.split(',').includes(day[0])" @click="toggleDay(row,day[0])">{{day[1]}}</button></div></article><button class="text-button" type="button" :disabled="s.radioSchedules.length>=20" @click="add"><AppIcon name="plus"/>Aggiungi fascia</button></div></div>` };
