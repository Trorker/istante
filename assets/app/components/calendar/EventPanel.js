import AppModal from '../common/AppModal.js';
import AppIcon from '../common/AppIcon.js';
import { useCalendarStore } from '../../stores/calendar-store.js';
export default { name: 'EventPanel', components: { AppModal, AppIcon }, emits: ['close'], setup() { const cal = useCalendarStore(); const fmt = ms => new Intl.DateTimeFormat('it-IT', { weekday: 'long', day: 'numeric', month: 'long', hour: '2-digit', minute: '2-digit' }).format(new Date(ms)); return { cal, fmt }; }, template: `<AppModal :title="cal.state.selectedEvent?.title||'Evento'" eyebrow="Calendario" @close="$emit('close')"><div v-if="cal.state.selectedEvent" class="event-detail"><p class="event-when">{{cal.state.selectedEvent.allDay?'Tutto il giorno':fmt(cal.state.selectedEvent.start)+' – '+fmt(cal.state.selectedEvent.end)}}</p><p v-if="cal.state.selectedEvent.location" class="event-info"><AppIcon name="location"/>{{cal.state.selectedEvent.location}}</p><p v-if="cal.state.selectedEvent.description" class="event-description">{{cal.state.selectedEvent.description}}</p><a v-if="cal.state.selectedEvent.url" class="secondary-button inline" :href="cal.state.selectedEvent.url" target="_blank" rel="noopener"><AppIcon name="open"/>Apri collegamento</a></div></AppModal>` };
